import styled from '@emotion/styled';

const StyledInput = styled.input`
  padding: 0;
  margin: 0;
  border: 0;
  background: 0;
  &:focus-visible {
    outline: none;
  }
`;

export default StyledInput;
